<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] AthMan - Andrew Stoddard</title>
    <link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .active.navitem {
            background-color: white !important;
            border-color: blueviolet !important;
            color: black;
            border-left: 8px purple solid;
        }
        .active  {
            background-color: white !important;
            border-color: blueviolet !important;
            color: black;
        }
        a {
            color: blueviolet;
        }
        .card {
            border: 2px purple solid;

        }
        a:hover { color: purple; }
        .jumbotron {
            background: blueviolet;
            color: white;

        }
        .btn-outline-primary {
            background: white;
            color: blueviolet;
            border: 1px purple solid;
        }
        .btn-outline-primary:hover {
            background: blueviolet;
            color: white;
            border: 1px white solid;
        }
        

        .list-group-item { background: white; }

    </style>


</head>
<body>
<header>
    <div class="jumbotron text-center">
        <h1>The AthMan<i class="fa fa-copyright fa-xs"></i></h1>
        <h5>Helping to Manage the World's Athletes</h5>
    </div>
</header>
<div class="container-fluid">
    <div class="row row-cols-sm-2">
        <div class="col-md-2">
            <nav>
                <ul class="list-group flex-sm-column flex-row">
                    <li class="list-group-item @(ViewData["Title"] == "Index" ? "active" : "") navitem">
                        <a asp-controller="Home" asp-action="Index">Home</a>
                    </li>
                    <li class="list-group-item @(ViewData["Title"] == "Items" ? "active" : "") navitem">
                        <a asp-controller="Items" asp-action="Items">Items</a>
                    </li>
                    <li class="list-group-item @(ViewData["Title"] == "Employees" ? "active" : "") navitem">
                        <a asp-controller="Employees" asp-action="Employees">Employees</a>
                    </li>
                    <li class="list-group-item @(ViewData["Title"] == "Clients" ? "active" : "") navitem">
                        <a asp-controller="Clients" asp-action="Clients">Clients</a>
                    </li>
                    <li class="list-group-item @(ViewData["Title"] == "Incidents" ? "active" : "") navitem">
                        <a asp-controller="Incidents" asp-action="Incidents">Incidents</a>
                    </li>
                </ul>

            </nav>
        </div>
        <div class="col-md-10">
            @RenderBody()

        </div>

    </div>
    @if (TempData["message"] != null)
    {
        <div class="alert alert-secondary w-25 fixed-bottom float-right" style="margin-left: 20px" role="alert">
            @TempData["message"]
        </div>
    }

</div>



</body>
</html>