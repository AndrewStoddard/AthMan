
@{
    ViewData["Title"] = "Items";
}

<div class="row row-cols-1 row-cols-lg-5 row-cols-sm-2 row-cols-md-4">
    @foreach (Item item in ViewBag.Items)
    {
        <a asp-action="View" asp-controller="Items" asp-route-itemId="@item.ItemID">
            <div class="card h-100 col">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text">@item.ReleaseDate - @item.YearlyPrice</p>
                </div>
            </div>
        </a>
    }

</div>
<div class="container-fluid">
    <a class="btn btn-primary" asp-action="Add" asp-controller="Items">Add New</a>
</div>